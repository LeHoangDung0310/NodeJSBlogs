<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= locals.title %></title>
    <meta name="description" content="<%= locals.description %>" />
    <link rel="stylesheet" href="/css/style.css" />
    <!-- <link rel="stylesheet" href="./admin.css"> -->
    <script type="text/javascript" defer src="/js/script.js"></script>

    <!-- Thêm link CSS của Toastr -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css"
      rel="stylesheet"
    />

    <!-- Thêm script JavaScript của Toastr -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
    <link
      href="https://cdn.quilljs.com/1.3.7/quill.snow.css"
      rel="stylesheet"
    />
    <script src="https://cdn.quilljs.com/1.3.7/quill.min.js"></script>


    <style>
      .article {
          max-width: 800px; /* Giới hạn độ rộng của bài viết */
          margin: 0 auto; /* Căn giữa nội dung bài viết */
          padding: 20px;
      }
  
      .images,
      .videos {
          text-align: center; /* Căn giữa các phần tử hình ảnh và video */
          margin-top: 20px;
      }
  
      .images img,
      .videos video {
          max-width: 100%; /* Đảm bảo ảnh/video không vượt quá kích thước của khung chứa */
          height: auto; /* Duy trì tỷ lệ khung hình của ảnh/video */
          margin: 10px 0; /* Thêm khoảng cách giữa các ảnh/video */
      }
  
      .videos video {
          max-width: 100%; /* Đảm bảo video không vượt quá kích thước khung chứa */
          width: 100%; /* Đảm bảo video chiếm toàn bộ chiều rộng có sẵn */
          height: auto; /* Duy trì tỷ lệ video */
      }
  </style>
  </head>
 

  <body>
    <div class="container">
      <% if (locals.isLoggedIn) { %>
      <!-- Sử dụng header cho admin đã đăng nhập -->
      <%- include('../partials/header_admin.ejs') %> <% } else { %>
      <!-- Sử dụng header mặc định hoặc header dành cho chưa đăng nhập -->
      <%- include('../partials/header_default.ejs') %> <% } %>
      <main class="main"><%- body %></main>
      <%- include('../partials/footer.ejs') %>
    </div>

    <script>
      // Cấu hình Toastr
      toastr.options = {
        closeButton: true,
        progressBar: true,
        positionClass: "toast-top-right", // Vị trí hiển thị toast
        timeOut: "5000", // Thời gian hiển thị toast (5000ms = 5s)
      };
    </script>
  </body>
</html>
